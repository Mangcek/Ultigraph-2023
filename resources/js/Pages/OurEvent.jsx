import { Link, Head } from '@inertiajs/react';

export default function OurEvent({ auth, laravelVersion, phpVersion }) {
    return (
        <>
            <div>INI PAGE OUR EVENT</div>
        </>
    );
}